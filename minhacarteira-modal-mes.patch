diff --git a/cal/views/views_transacao.py b/cal/views/views_transacao.py
index 70066a7..ec93135 100644
--- a/cal/views/views_transacao.py
+++ b/cal/views/views_transacao.py
@@ -1,4 +1,5 @@
 import uuid
+import calendar
 from datetime import date
 from decimal import Decimal, InvalidOperation
 
@@ -102,7 +103,7 @@ def transacao_rapida(request):
     """
     Endpoint enxuto para o modal de registro rápido (menos cliques).
 
-    Campos essenciais: título, valor, tipo, categoria (opcional).
+    Campos essenciais: título, valor, tipo, mês do lançamento e categoria (opcional).
     Campos avançados (opcionais, ficam escondidos atrás de um "+ opções" no
     modal): cartão e parcelas. Quando informados, reaproveita a mesma regra
     de fatura usada no formulário completo (calcular_proxima_fatura) para
@@ -113,6 +114,7 @@ def transacao_rapida(request):
     categoria_id = request.POST.get('categoria') or None
     cartao_id = request.POST.get('cartao') or None
     parcelas_raw = request.POST.get('parcelas') or '1'
+    mes_lancamento = (request.POST.get('mes_lancamento') or '').strip()
 
     erros = []
     if not titulo:
@@ -149,13 +151,26 @@ def transacao_rapida(request):
         parcelas = 1
         erros.append('Número de parcelas inválido.')
 
+    hoje = date.today()
+    data_lancamento = hoje
+    if mes_lancamento:
+        try:
+            ano, mes = (int(parte) for parte in mes_lancamento.split('-', 1))
+            if ano < 2000 or not 1 <= mes <= 12:
+                raise ValueError
+            # Mantém o dia escolhido quando possível; em fevereiro e meses
+            # menores, usa o último dia válido do mês.
+            dia = min(hoje.day, calendar.monthrange(ano, mes)[1])
+            data_lancamento = date(ano, mes, dia)
+        except (ValueError, TypeError):
+            erros.append('Selecione um mês válido para o lançamento.')
+
     if erros:
         return JsonResponse({'status': 'error', 'errors': erros}, status=400)
 
-    hoje = date.today()
     valor_parcela = valor_total.quantize(Decimal('0.01'))
 
-    data_base_parcela = calcular_proxima_fatura(hoje) if cartao else hoje
+    data_base_parcela = calcular_proxima_fatura(data_lancamento) if cartao else data_lancamento
 
     grupo_id = str(uuid.uuid4()) if parcelas > 1 else None
 
diff --git a/templates/base.html b/templates/base.html
index 10e0264..c101ada 100644
--- a/templates/base.html
+++ b/templates/base.html
@@ -192,6 +192,15 @@
             <div class="modal-body">
               <div id="quickAddErro" class="alert alert-danger d-none py-2 mb-3"></div>
 
+              <!-- O mês fica explícito antes dos demais dados do lançamento. -->
+              <div class="campo-card mb-3 border-primary">
+                <label for="quickMesLancamento" class="fw-bold text-primary">
+                  <i class="bi bi-calendar3"></i> Mês do lançamento
+                </label>
+                <input type="month" class="form-control" id="quickMesLancamento" name="mes_lancamento" required>
+                <div class="form-text">Escolha o mês do lançamento. Compras no cartão seguem a regra da próxima fatura.</div>
+              </div>
+
               <!-- Step 1: Tipo (Entrada/Saída) -->
               <div class="tipo-selector" role="group" aria-label="Tipo de transação">
                 {% for tipo in tipos_quick_add %}
@@ -424,10 +433,17 @@ $(document).ready(function () {
 
   const erroBox = document.getElementById('quickAddErro');
   const submitBtn = document.getElementById('quickAddSubmit');
+  const mesLancamento = document.getElementById('quickMesLancamento');
+
+  function mesAtual() {
+    const hoje = new Date();
+    return `${hoje.getFullYear()}-${String(hoje.getMonth() + 1).padStart(2, '0')}`;
+  }
 
   // Ao abrir o modal: foco direto no valor (é o dado que a pessoa mais digita)
   // e restaura a última categoria/tipo usados, pra não ter que escolher de novo toda vez.
   modalEl.addEventListener('shown.bs.modal', function () {
+    if (!mesLancamento.value) mesLancamento.value = mesAtual();
     document.getElementById('quickValor').focus();
     erroBox.classList.add('d-none');
 
