diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000..0000000
--- /dev/null
+++ b/.env.example
@@ -0,0 +1,52 @@
+# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
+# MINHACARTEIRA — CONFIGURAÇÃO DE AMBIENTE
+# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
+
+# ══════════════════════════════════════════════════
+# DESENVOLVIMENTO LOCAL (HTTP)
+# ══════════════════════════════════════════════════
+DEBUG=True
+SECRET_KEY=django-insecure-MUDE-ESTE-VALOR-EM-PRODUCAO-k3y9x2m5p8q
+ALLOWED_HOSTS=localhost,127.0.0.1
+CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
+
+# Desabilitar HTTPS no desenvolvimento local
+SECURE_SSL_REDIRECT=False
+SESSION_COOKIE_SECURE=False
+CSRF_COOKIE_SECURE=False
+SECURE_HSTS_SECONDS=0
+
+# ══════════════════════════════════════════════════
+# PRODUÇÃO (HTTPS)
+# ══════════════════════════════════════════════════
+# Descomentar e ajustar para produção:
+
+# DEBUG=False
+# SECRET_KEY=GERE-UMA-CHAVE-FORTE-COM-python-manage.py-shell-c-"from-django.core.management.utils-import-get_random_secret_key;-print(get_random_secret_key())"
+# ALLOWED_HOSTS=seudominio.com,www.seudominio.com
+# CSRF_TRUSTED_ORIGINS=https://seudominio.com,https://www.seudominio.com
+# SECURE_SSL_REDIRECT=True
+# SESSION_COOKIE_SECURE=True
+# CSRF_COOKIE_SECURE=True
+# SECURE_HSTS_SECONDS=31536000
+
+# ══════════════════════════════════════════════════
+# BANCO DE DADOS (padrão: SQLite local)
+# ══════════════════════════════════════════════════
+DATABASE_URL=sqlite:///db.sqlite3
+
+# Produção (PostgreSQL exemplo):
+# DATABASE_URL=postgres://usuario:senha@host:5432/minhacarteira
+
+# ══════════════════════════════════════════════════
+# CELERY (tarefas assíncronas)
+# ══════════════════════════════════════════════════
+CELERY_BROKER_URL=redis://localhost:6379/0
+CELERY_RESULT_BACKEND=redis://localhost:6379/0
+
+# ══════════════════════════════════════════════════
+# CACHE (opcional)
+# ══════════════════════════════════════════════════
+CACHE_URL=locmem://
+# Produção:
+# CACHE_URL=redis://localhost:6379/1
diff --git a/.gitignore b/.gitignore
index 68bc17f..2b9c808 100644
--- a/.gitignore
+++ b/.gitignore
@@ -120,6 +120,7 @@ celerybeat.pid
 *.sage.py
 
 # Environments
+.env
 .env
 .venv
 env/
diff --git a/requirements.txt b/requirements.txt
index a2a76a5..c8d6e1f 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -10,3 +10,6 @@ requests==2.31.0
 gunicorn==21.2.0
 psycopg2-binary==2.9.9
 whitenoise==6.6.0
+django-environ==0.11.2
+django-ratelimit==4.1.0
+bleach==6.1.0
diff --git a/minhacarteira/settings.py b/minhacarteira/settings.py
index 7f8a9c1..8f2c9d3 100644
--- a/minhacarteira/settings.py
+++ b/minhacarteira/settings.py
@@ -1,28 +1,49 @@
-"""
-Django settings for minhacarteira project.
-
-Generated by 'django-admin startproject' using Django 4.2.7.
-
-For more information on this file, see
-https://docs.djangoproject.com/en/4.2/topics/settings/
-
-For the full list of settings and their values, see
-https://docs.djangoproject.com/en/4.2/ref/settings/
-"""
-
 from pathlib import Path
+import environ
+import os
 
 # Build paths inside the project like this: BASE_DIR / 'subdir'.
 BASE_DIR = Path(__file__).resolve().parent.parent
 
+# Inicializar django-environ
+env = environ.Env(
+    DEBUG=(bool, False),
+    SECRET_KEY=(str, ''),
+    ALLOWED_HOSTS=(list, ['localhost', '127.0.0.1']),
+    CSRF_TRUSTED_ORIGINS=(list, []),
+    SECURE_SSL_REDIRECT=(bool, False),
+    SESSION_COOKIE_SECURE=(bool, False),
+    CSRF_COOKIE_SECURE=(bool, False),
+    SECURE_HSTS_SECONDS=(int, 0),
+    DATABASE_URL=(str, f'sqlite:///{BASE_DIR / "db.sqlite3"}'),
+    CELERY_BROKER_URL=(str, 'redis://localhost:6379/0'),
+    CACHE_URL=(str, 'locmem://'),
+)
+
+# Ler arquivo .env (se existir)
+env_file = BASE_DIR / '.env'
+if env_file.exists():
+    environ.Env.read_env(env_file)
 
-# Quick-start development settings - unsuitable for production
-# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/
+# SECURITY WARNING: keep the secret key used in production secret!
+SECRET_KEY = env('SECRET_KEY')
+if not SECRET_KEY or SECRET_KEY.startswith('django-insecure-'):
+    if not env('DEBUG'):
+        raise ValueError(
+            "SECRET_KEY deve ser definida em produção. "
+            "Gere uma com: python manage.py shell -c "
+            '"from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"'
+        )
 
-# SECURITY WARNING: keep the secret key used in production secret!
-SECRET_KEY = 'django-insecure-k3y#9@k_2m5p8q7r&t$w^z%a1b*c4d6e8f!g'
+# SECURITY WARNING: don't run with debug turned on in production!
+DEBUG = env('DEBUG')
 
-# SECURITY WARNING: don't run with debug turned on in production!
-DEBUG = True
+ALLOWED_HOSTS = env.list('ALLOWED_HOSTS')
+CSRF_TRUSTED_ORIGINS = env.list('CSRF_TRUSTED_ORIGINS')
 
-ALLOWED_HOSTS = []
+# Segurança de cookies (condicional por ambiente)
+SESSION_COOKIE_SECURE = env('SESSION_COOKIE_SECURE')
+SESSION_COOKIE_HTTPONLY = True
+SESSION_COOKIE_SAMESITE = 'Lax'
+CSRF_COOKIE_SECURE = env('CSRF_COOKIE_SECURE')
+CSRF_COOKIE_SAMESITE = 'Lax'
+
+# Headers de segurança
+SECURE_SSL_REDIRECT = env('SECURE_SSL_REDIRECT')
+SECURE_HSTS_SECONDS = env('SECURE_HSTS_SECONDS')
+SECURE_CONTENT_TYPE_NOSNIFF = True
+SECURE_REFERRER_POLICY = 'strict-origin-when-cross-origin'
+X_FRAME_OPTIONS = 'DENY'
 
 
 # Application definition
@@ -75,10 +96,10 @@ WSGI_APPLICATION = 'minhacarteira.wsgi.application'
 # Database
 # https://docs.djangoproject.com/en/4.2/ref/settings/#databases
 
+import dj_database_url
+
 DATABASES = {
-    'default': {
-        'ENGINE': 'django.db.backends.sqlite3',
-        'NAME': BASE_DIR / 'db.sqlite3',
-    }
+    'default': dj_database_url.parse(env('DATABASE_URL'), conn_max_age=600, conn_health_checks=True)
 }
 
 
@@ -125,3 +146,15 @@ STATICFILES_DIRS = [
 # Default primary key field type
 # https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field
 
 DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
+
+# Cache
+CACHES = {'default': env.cache('CACHE_URL')}
+
+# Celery
+CELERY_BROKER_URL = env('CELERY_BROKER_URL')
+CELERY_RESULT_BACKEND = env('CELERY_BROKER_URL').replace('/0', '/1')
+CELERY_ACCEPT_CONTENT = ['json']
+CELERY_TASK_SERIALIZER = 'json'
+CELERY_RESULT_SERIALIZER = 'json'
+CELERY_TIMEZONE = 'America/Sao_Paulo'
diff --git a/cal/forms.py b/cal/forms.py
index 5f3a892..8bc4d7e 100644
--- a/cal/forms.py
+++ b/cal/forms.py
@@ -1,8 +1,8 @@
 from django import forms
-import json
 from django.contrib.auth.forms import UserCreationForm
 from django.contrib.auth.models import User
 from django.core.exceptions import ValidationError
+from django.core.validators import FileExtensionValidator
 from .models import Transaction, Category, Card, RecurringTransaction
 
 
@@ -45,6 +45,35 @@ class TransactionForm(forms.ModelForm):
             'recurring': forms.CheckboxInput(attrs={'class': 'form-check-input'}),
         }
     
+    receipt = forms.FileField(
+        required=False,
+        validators=[
+            FileExtensionValidator(
+                allowed_extensions=['pdf', 'jpg', 'jpeg', 'png'],
+                message="Apenas PDF, JPG ou PNG são permitidos."
+            )
+        ],
+        help_text="Tamanho máximo: 5 MB",
+        widget=forms.FileInput(attrs={'class': 'form-control', 'accept': '.pdf,.jpg,.jpeg,.png'})
+    )
+    
+    def clean_receipt(self):
+        """Valida upload de comprovante."""
+        import mimetypes
+        
+        file = self.cleaned_data.get('receipt')
+        if file:
+            # Validação de tamanho (5 MB)
+            if file.size > 5 * 1024 * 1024:
+                raise ValidationError("Arquivo muito grande (máximo: 5 MB)")
+            
+            # Validação de MIME type
+            mime, _ = mimetypes.guess_type(file.name)
+            allowed = ['application/pdf', 'image/jpeg', 'image/png']
+            if mime not in allowed:
+                raise ValidationError("Tipo de arquivo inválido")
+        
+        return file
+    
     def __init__(self, *args, **kwargs):
         self.user = kwargs.pop('user', None)
         super().__init__(*args, **kwargs)
diff --git a/cal/views.py b/cal/views.py
index 8f3c5e1..d2a8f45 100644
--- a/cal/views.py
+++ b/cal/views.py
@@ -1,11 +1,13 @@
 from django.shortcuts import render, redirect, get_object_or_404
 from django.contrib.auth.decorators import login_required
 from django.contrib.auth import login, authenticate, logout
 from django.contrib.auth.forms import UserCreationForm, AuthenticationForm
-from django.db.models import Q, Sum, Count
+from django.db.models import Sum, Count
 from django.http import JsonResponse, HttpResponse
 from django.views.decorators.http import require_http_methods
 from django.core.paginator import Paginator
+from django_ratelimit.decorators import ratelimit
+from django_ratelimit.exceptions import Ratelimited
 from django.utils import timezone
 from datetime import datetime, timedelta
 from decimal import Decimal
@@ -20,11 +22,23 @@ logger = logging.getLogger(__name__)
 
 
 # Autenticação
+@ratelimit(key='ip', rate='5/m', method='POST', block=True)
 def login_view(request):
+    """
+    Login com rate limiting (5 tentativas/minuto por IP).
+    
+    Protege contra brute force sem impactar uso legítimo.
+    """
     if request.method == 'POST':
         form = AuthenticationForm(data=request.POST)
         if form.is_valid():
             user = form.get_user()
             login(request, user)
+            logger.info(f"Login: {user.username}")
             return redirect('dashboard')
+        else:
+            logger.warning(
+                f"Falha no login: {request.POST.get('username', 'unknown')} "
+                f"IP: {request.META.get('REMOTE_ADDR')}"
+            )
     else:
         form = AuthenticationForm()
@@ -32,6 +46,7 @@ def login_view(request):
 
 
+@ratelimit(key='ip', rate='3/h', method='POST', block=True)
 def register_view(request):
     if request.method == 'POST':
         form = UserCreationForm(request.POST)
@@ -39,6 +54,7 @@ def register_view(request):
             user = form.save()
             login(request, user)
+            logger.info(f"Novo usuário: {user.username}")
             return redirect('dashboard')
     else:
         form = UserCreationForm()
@@ -136,7 +152,7 @@ def transaction_form(request, pk=None):
 
 @login_required
 def transaction_delete(request, pk):
-    transaction = get_object_or_404(Transaction, pk=pk)
+    transaction = get_object_or_404(Transaction, pk=pk, user=request.user)
     
     if request.method == 'POST':
         transaction.delete()
@@ -151,7 +167,7 @@ def transaction_delete(request, pk):
 
 @login_required
 def category_form(request, pk=None):
-    category = get_object_or_404(Category, pk=pk) if pk else None
+    category = get_object_or_404(Category, pk=pk, user=request.user) if pk else None
     
     if request.method == 'POST':
         form = CategoryForm(request.POST, instance=category)
@@ -171,7 +187,7 @@ def category_list(request):
 
 @login_required
 def category_delete(request, pk):
-    category = get_object_or_404(Category, pk=pk)
+    category = get_object_or_404(Category, pk=pk, user=request.user)
     
     if request.method == 'POST':
         category.delete()
@@ -186,7 +202,7 @@ def category_delete(request, pk):
 
 @login_required
 def card_form(request, pk=None):
-    card = get_object_or_404(Card, pk=pk) if pk else None
+    card = get_object_or_404(Card, pk=pk, user=request.user) if pk else None
     
     if request.method == 'POST':
         form = CardForm(request.POST, instance=card)
@@ -206,7 +222,7 @@ def card_list(request):
 
 @login_required
 def card_delete(request, pk):
-    card = get_object_or_404(Card, pk=pk)
+    card = get_object_or_404(Card, pk=pk, user=request.user)
     
     if request.method == 'POST':
         card.delete()
@@ -221,7 +237,7 @@ def card_delete(request, pk):
 
 @login_required
 def recurring_form(request, pk=None):
-    recurring = get_object_or_404(RecurringTransaction, pk=pk) if pk else None
+    recurring = get_object_or_404(RecurringTransaction, pk=pk, user=request.user) if pk else None
     
     if request.method == 'POST':
         form = RecurringTransactionForm(request.POST, instance=recurring)
@@ -241,7 +257,7 @@ def recurring_list(request):
 
 @login_required
 def recurring_delete(request, pk):
-    recurring = get_object_or_404(RecurringTransaction, pk=pk)
+    recurring = get_object_or_404(RecurringTransaction, pk=pk, user=request.user)
     
     if request.method == 'POST':
         recurring.delete()
@@ -287,8 +303,9 @@ def api_transactions(request):
             'card': t.card.name if t.card else None,
         } for t in transactions]
         return JsonResponse({'transactions': data})
-    except Exception as e:
-        return JsonResponse({'error': str(e)}, status=500)
+    except Exception:
+        logger.exception("Erro ao buscar transações")
+        return JsonResponse({'error': 'Erro ao buscar transações'}, status=500)
 
 
 @login_required
diff --git a/templates/cal/dashboard.html b/templates/cal/dashboard.html
index 4a8f123..d7e9c24 100644
--- a/templates/cal/dashboard.html
+++ b/templates/cal/dashboard.html
@@ -86,7 +86,7 @@
                 <td>{{ transaction.category.name }}</td>
                 <td>{{ transaction.card.name|default:"-" }}</td>
                 <td>
-                    {{ transaction.description|safe }}
+                    {{ transaction.description|escape|linebreaks }}
                 </td>
                 <td>
                     <a href="{% url 'transaction_form' transaction.pk %}" class="btn btn-sm btn-primary">Editar</a>